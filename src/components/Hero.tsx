"use client";
import { FC } from "react";
import ComputersCanvas from "./Computer3D";
import Link from "next/link";

interface HeroProps {}

const Hero: FC<HeroProps> = ({}) => {
  return <section className="relative w-full h-screen mx-auto">hero</section>;
};

export default Hero;
